%h1 Welcome employees
%h2 If you do not yet have an account, sign up
- if user_signed_in?
  = link_to "Upload a cart", carts_path
  %br
  = link_to "Log Out", logout_path, method: :delete
- else
  = link_to "Log In", login_path
  %br
  = link_to "Sign Up", register_path
